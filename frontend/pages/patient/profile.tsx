import Navbar from '../../components/Navbar';

const PatientProfile = () => (
  <div>
    <Navbar />
    <h1>Patient Profile</h1>
    <p>Select a category from the menu to view or update profile details.</p>
  </div>
);

export default PatientProfile;
